{
  "name": "z0k",
  "version": "0.0.15",
  "description": "zok library for zod to snarkyjs struct transformation",
  "bin": {
    "zok": "build/index.js"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "start": "rm -rf src/structs.ts && npm run build && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "snarkyjs": "^0.10.1",
    "typescript": "^5.0.4",
    "zod": "^3.21.4",
    "zod-to-json-schema": "3.21.2"
  },
  "devDependencies": {
    "@types/node": "^20.1.1",
    "ts-creator": "^1.2.5"
  }
}
