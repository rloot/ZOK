{
  "name": "zok",
  "version": "0.0.1",
  "description": "zok library for zod to snarkyjs struct transformation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "start": "rm -rf src/structs.ts && npm run build &&  node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "snarkyjs": "0.9.8",
    "typescript": "^5.0.4",
    "zod": "^3.21.4",
    "zod-to-json-schema": "^3.20.4"
  },
  "devDependencies": {
    "@types/node": "^20.1.1",
    "eslint-plugin-snarkyjs": "^0.3.0",
    "ts-creator": "^1.2.5"
  }
}
